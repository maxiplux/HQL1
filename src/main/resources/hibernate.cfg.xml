<?xml version="1.0" encoding="windows-1252" ?>
 <!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
 "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- HSQL DB running on localhost -->
        <property name="connection.url">jdbc:mysql://localhost/midterm</property>
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property>
        <property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>

        <!-- Use the Hibernate built-in pool for tests. -->
        <property name="connection.pool_size">2</property>

        <!-- Show all SQL DML executed by Hibernate -->
        <property name="show_sql">true</property>
        <property name="use_sql_comments">true</property>
        <property name="hibernate.format_sql">true</property>
        
        <!-- Always drop and recreate the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>
        <!-- Needed since Hibernate 5 to generate identity columns properly -->
        <property name="hibernate.id.new_generator_mappings">true</property>

        <property name="hibernate.connection.autocommit">true</property>


        <!-- Disable second-level cache provider -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <!-- Mapping files -->

        <mapping class="cs544.hql1.midterm.Tree"/>
        <mapping class="cs544.hql1.midterm.CreditCard"/>
        <mapping class="cs544.hql1.midterm.Customer"/>

        <mapping class="cs544.hql1.midterm.Deciduous"/>
        <mapping class="cs544.hql1.midterm.Evergreen"/>
        <mapping class="cs544.hql1.midterm.Treatment"/>



    </session-factory>
</hibernate-configuration>
